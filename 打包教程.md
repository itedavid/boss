# BossHelper 重新打包教程

## 一、准备工作（只需做一次）

1. 安装 Go：打开 <https://go.dev/dl/> ，下载 Windows 版安装包，一路默认安装。
2. 装完后**新开**一个 CMD 或 PowerShell 窗口（旧窗口读不到新的环境变量），输入：

   ```
   go version
   ```

   能显示 `go version go1.2x.x windows/amd64` 就说明装好了。
3. 项目目录：`D:\Codes\ai\boss`

> 本项目**没有任何第三方依赖**（`go.mod` 里没有 require），所以编译时**不需要联网**，
> 也不需要 `go mod download` 之类的操作。

---

## 二、打包（两种方式，任选一种）

### 方式 A：双击（最省事）

进入 `D:\Codes\ai\boss`，双击 **`build.cmd`**。

成功时会打印一行：

```
OK: D:\Codes\ai\boss\BossHelper.exe  3001344 bytes
```

### 方式 B：命令行

```bat
cd /d D:\Codes\ai\boss
go build -trimpath -ldflags="-s -w -H=windowsgui" -o BossHelper.exe .
```

---

## 三、命令参数是什么意思

| 参数 | 作用 |
| --- | --- |
| `go build` | 编译当前目录的 Go 程序 |
| `-trimpath` | 去掉编译产物里的本机路径，体积更小、也不泄露目录结构 |
| `-s` | 去掉符号表 |
| `-w` | 去掉调试信息 |
| `-H=windowsgui` | 生成 GUI 程序，**不弹黑色命令行窗口**（很重要） |
| `-o BossHelper.exe` | 输出的文件名 |
| `.` | 编译当前目录 |

> `-s -w` 两个加起来大概能小 30% 左右；不想要也可以去掉，只是体积变大。

---

## 四、怎么确认打包成功了

1. `D:\Codes\ai\boss` 目录里出现 **`BossHelper.exe`**，大小约 **2.9 MB**。
2. 双击能正常打开窗口，标题是 `Boss Helper V0.6`。
3. 程序会在 **exe 所在目录**读写 `config.json`（把 exe 复制到别的地方，配置也跟着过去）。

---

## 五、开发时临时运行（不打包）

```bat
cd /d D:\Codes\ai\boss
go run .
```

`go run` 生成的临时 exe 在系统临时目录里，所以这种情况下 `config.json` 会读写在项目目录。

---

## 六、常见问题

1. **提示 `'go' 不是内部或外部命令`**
   Go 没装好，或者装完没重新开窗口。重开一个 CMD 再试。

2. **编译报错里有 `Access denied` / `being used by another process`**
   `BossHelper.exe` 正在运行，先关掉它再打包。

3. **Windows Defender 提示病毒**
   未签名的 exe 很常见，属于误报。在 Defender 里加入信任/排除项即可。

4. **打包出来的程序带一个黑色命令行窗口**
   说明少了 `-H=windowsgui`，检查命令是否完整。

5. **想改窗口标题或版本号**
   改 `main.go` 里这一行的字符串：

   ```go
   utf16ptr("Boss Helper V0.6"),
   ```

6. **想改默认参数**（比如 OCR 轮询间隔）
   - 设备识别间隔 / 连续确认次数：`detect.go` 顶部的 `detectInterval`、`detectConfirm`
   - 点击随机偏移：`auto.go` 顶部的 `clickJitterPx`
   - OCR 放大目标高度：`image.go` 顶部的 `ocrTargetH`

   改完按第七节重新打包即可。

---

## 七、改完代码后重新打包（三步）

1. 关掉正在运行的 `BossHelper.exe`
2. 双击 `build.cmd`（或执行方式 B 的命令）
3. 看到 `OK: ... BossHelper.exe ... bytes` 就成功了，直接双击运行